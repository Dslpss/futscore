# =================================
# FutScore Backend - Environment Variables
# =================================

# MongoDB Connection
# Replace with your actual MongoDB Atlas connection string
MONGO_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/YOUR_DATABASE?retryWrites=true&w=majority

# JWT Secret (use a long random string in production)
JWT_SECRET=your-super-secret-jwt-key-here

# =================================
# Firebase Configuration (for Auth migration)
# =================================

# Option 1: Service Account JSON (recommended for Railway)
# Get this from Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# Paste the entire JSON as a single line (minified)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project-id",...}

# Option 2: Just the project ID (limited functionality)
# FIREBASE_PROJECT_ID=your-project-id

# =================================
# Email Configuration for Password Recovery
# (Only needed if Firebase is not configured)
# =================================

# Option 1: Gmail (easier setup)
# Note: For Gmail, you need to:
# 1. Enable 2-Factor Authentication on your Google account
# 2. Generate an "App Password" at https://myaccount.google.com/apppasswords
# 3. Use that App Password as EMAIL_PASS
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password

# Option 2: Custom SMTP (more flexible)
# SMTP_HOST=smtp.yourprovider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password

# =================================
# App Configuration
# =================================

# App URL for password reset links (optional, used for deep links)
# APP_URL=https://yourapp.com

# Railway will set this automatically
# RAILWAY_PUBLIC_DOMAIN=your-app.up.railway.app

# Port (Railway sets this automatically)
PORT=5000

# =================================
# Cakto Payment Gateway
# =================================

# Cakto API Credentials (get from Cakto Dashboard > Integrations > API Keys)
CAKTO_CLIENT_ID=your-cakto-client-id
CAKTO_CLIENT_SECRET=your-cakto-client-secret

# Product checkout link (get from Cakto Dashboard > Products)
CAKTO_PRODUCT_LINK=https://pay.cakto.com.br/your-product-code

# Webhook secret (optional, for signature validation)
# Get from Cakto Dashboard > Integrations > Webhooks
CAKTO_WEBHOOK_SECRET=your-webhook-secret
